{
  "name": "@mizpah-pulse/stellar",
  "version": "0.0.1",
  "private": true,
  "description": "Stellar SDK integration utilities for MizpahPulse",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@mizpah-pulse/types": "*",
    "@stellar/stellar-sdk": "^17.0.1",
    "stellar-sdk": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
