export function matchSwitch<T extends string, R>(value: T, cases: Record<T, R | (() => R)>, defaultCase?: R | (() => R)): R { const c = cases[value]; if (c !== undefined) return typeof c === 'function' ? (c as () => R)() : c; if (defaultCase !== undefined) return typeof defaultCase === 'function' ? (defaultCase as () => R)() : defaultCase; throw new Error(`No match for: ${value}`); }
